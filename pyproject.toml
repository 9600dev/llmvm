[tool.poetry]
name = "llmagents"
version = "0.1.0"
description = ""
authors = ["Sonny Bonds <contact@9600dev.com>"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"
openai = "^0.27.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
