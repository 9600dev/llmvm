[system_message]
You are a helpful assistant.

[user_message]

The previous messages above contains data that I need to convert into a list, and then perform a task on that list. You should iterate over an appropriately generated list of data and apply the following task, query or transformation:

Task:

{{message}}
