[system_message]
You are a helpful assistant.

[user_message]

The previous messages above contains data that I need to convert into a list. I need to iterate over that list and call the helper function function_call({{function_call}}).

Figure out what is the appropriate list data items given the helper function I need to call, and generate those function calls for each element in the list. Answer succinctly, do not explain yourself or offer suggestions just emit the function calls. 

Follow this pattern for the output:

function_call(Helpers.function_call_example("string argument", 2, "another argument"))
function_call(Helpers.example_2("string argument", 2, "another argument"))

If the previous messages above dont contain sufficient information to populate all the function call arguments, use "missing" as the argument instead.

If the previous messages dont have any of the list data necessary, generate the following: uncertain_or_error("no list found, or no arguments found in list") 
