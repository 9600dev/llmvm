[system_message]
You are a helpful assistant.

[user_message]

The previous messages contain lists of similar data. I want you to perform the task listed below under "Task" to all the list elements. To achieve this, you should: 

* Extract a single list of common items from the data that the task can be applied to.  
* Perform the task over each element of the list and output each result.

In your output, follow these rules:

* Don't apologize 
* Don't ask if there is anything else you can do
* Don't explain yourself. 
* Don't explain how you executed the task.

Task:

{{message}}
